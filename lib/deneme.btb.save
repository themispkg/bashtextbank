export bank="deneme"

export encoded_data=(
	"deneme:bir"
	"test:bir"
	"test:deneme:b"
)

parse_data() {
	read table file 1//:/ }
	echo "${table}"
}

for i in ${encoded_data[@]} ; do
	parse_data "${i}"
done
